# Paths and build targets a document may name although they are not in
# the repo, read by tools/check-doc-refs.py. One glob per line; a line
# starting with "make " or "cabal " allows a target instead of a path.
# Text after "#" is the reason, and the reason is the point of the entry:
# an unexplained line here silently hides real drift, which is exactly
# what the checker exists to catch. When a named artifact lands, delete
# its entry rather than leaving it.
#
# Adding an entry is also the easy way to silently defang the checker, so
# re-run its docstring's non-vacuity test after touching this file.

# Named as a phantom rather than as a repo file. The inner sandbox
# bind-mounts /dev/null over eleven dotfile names in the session's own
# working directory, once per Bash call (CLAUDE.md's sandboxing notes),
# so this path's existence follows who is looking rather than what is in
# the repo: it resolved in a sandboxed run of this checker, failed in an
# unsandboxed one, and fails from a session rooted in a sibling checkout,
# which gets no phantoms at all. The entry is what makes the verdict the
# same everywhere. Of the eleven only this one needs it: the rest carry
# no extension this checker reads as a path, so they stay unclassified.
.mcp.json

# Superseded planning documents, named by the unified plan as history.
sdl2-to-wasm-parity-plan.md
wasm-frontend-haskell-alignment-design.md

# Foreign repositories, not resolvable locally.
compiler/GHC/*

# Emitted by the ghc-wasm-meta toolchain at build time, never committed
# here. `ghc_wasm_jsffi.mjs` is deliberately absent from this list: it is
# deployed into ../lambdahack.github.io, so the sibling policy resolves it
# for real, which beats an entry asserting it exists somewhere.
post-link.mjs

# Artifacts docs/wasm-frontend-unified-plan.md proposes but has not built
# yet (Phases 0-3). Each should disappear from here as its phase lands.
CellStyle.hs
Client/UI/Frontend/InputDecision.hs
Client/UI/Frontend/OverlayLayout.hs
cursor.ts
fixtures.json
overlay-core.ts
overlay-core.test.ts
palette.ts
run-wasm-game.mjs
test/OverlayLayoutUnitTests.hs
ts-src/src/generated/
GameDefinition/gen-src/GenTsTables.hs
frame-timing-core.test.ts
frame-timing-core.ts
fullscreen-core.test.ts
fullscreen-core.ts
make nodeDeployedBench
make smokeANSI
make smokeNodeBench
make smokeSdl
render-coherence.test.ts
screenshot-core.test.ts
screenshot-core.ts
test/CellStyleUnitTests.hs
test/DeterminismGoldenUnitTests.hs
test/FfiCoverageUnitTests.hs
test/FrontendContractUnitTests.hs
test/InputDecisionUnitTests.hs
test/WasmFileUnitTests.hs
ts-src/src/terminal-input.test.ts
url-options-core.test.ts
url-options-core.ts
make gen-ts
