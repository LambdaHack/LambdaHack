{
 "CONFIG": {},
 "RECORDS": [
  {
   "id": "check-doc-examples-01",
   "program": "check-doc-examples.py",
   "name": "run from docs/, the source read hung on cat $(...) and, with stdin closed, failed every name",
   "family": "environment-decides",
   "fix_rev": "325b3dcd3",
   "trigger": "a run from a subdirectory of the repository",
   "ok": "the script moves itself to the repository root first",
   "bug": "a hang, or every name reported missing",
   "proved": "asserted",
   "harm": "fired",
   "harm_count": 1,
   "discovery": "in-use",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here."
  },
  {
   "id": "check-doc-examples-02",
   "program": "check-doc-examples.py",
   "name": "a module header was read as a declaration of its own name, which was then reported as undefined",
   "family": "scan-for-parse",
   "fix_rev": "325b3dcd3",
   "trigger": "a fenced example opening with `module Name where`",
   "ok": "the module's own name is a doc-local name",
   "bug": "the module's name reported as a finding",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here."
  },
  {
   "id": "check-doc-examples-03",
   "program": "check-doc-examples.py",
   "name": "a fenced block declaring module Main was compared against the library sources it cannot be in",
   "family": "scan-for-parse",
   "fix_rev": "325b3dcd3",
   "trigger": "an example that is a whole program, `module Main`",
   "ok": "the block is skipped, and the skip has a control",
   "bug": "every name of the program reported as absent from the sources",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here."
  },
  {
   "id": "check-doc-refs-01",
   "program": "check-doc-refs.py",
   "name": "run from a subdirectory, the reference check answered for a different tree, every checker having said run from the root and none having enforced it",
   "family": "environment-decides",
   "fix_rev": "325b3dcd3",
   "trigger": "a run from a subdirectory of the repository",
   "ok": "the script moves itself to the repository root first",
   "bug": "references resolved against the wrong directory",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here."
  },
  {
   "id": "check-doc-refs-02",
   "program": "check-doc-refs.py",
   "name": "with no wrap80 on PATH the spans were read wrapped and the run said nothing about it",
   "family": "quiet-failure",
   "fix_rev": "325b3dcd3",
   "trigger": "a PATH without wrap80",
   "ok": "the run says that spans were read wrapped",
   "bug": "silence, and a phrase a line break falls inside is missed",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here."
  },
  {
   "id": "check-doc-refs-03",
   "program": "check-doc-refs.py",
   "name": "a backticked reference a wrap broke across two lines left the check's sight",
   "family": "scan-for-parse",
   "fix_rev": "325b3dcd3",
   "trigger": "reflowing README.md so a break falls inside `ssum0 . foo`",
   "ok": "the document is read unwrapped",
   "bug": "two of README's references unchecked, silently",
   "proved": "asserted",
   "harm": "fired",
   "harm_count": 2,
   "discovery": "in-use",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here."
  },
  {
   "id": "check-doc-refs-04",
   "program": "check-doc-refs.py",
   "name": "NOT_IN_PATH was taken to mark URLs, which it does not, so a URL ending in a slash failed through dir_shaped",
   "family": "scan-for-parse",
   "fix_rev": "325b3dcd3",
   "trigger": "a URL ending in a slash",
   "ok": "URLs are recognised by their scheme",
   "bug": "the URL reported as a missing directory",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here."
  },
  {
   "id": "check-doc-wrap-01",
   "program": "check-doc-wrap.py",
   "name": "committed_form met the missing tool first and called the document at neither fixed point, so the BLOCKED branch sat unreachable",
   "family": "vacuous-check",
   "fix_rev": "325b3dcd3",
   "trigger": "a PATH without wrap80",
   "ok": "BLOCKED at exit 2",
   "bug": "a verdict about the document's form, from a tool that could not read it",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "The program arrived here already fixed, in 325b3dcd3, so this tree has no baseline to replay and the record carries no case; the mutant in tools/mutants.py is what holds the fix. Recorded so the shape is not reintroduced when the twin copies are next synced."
  },
  {
   "id": "check-doc-wrap-02",
   "program": "check-doc-wrap.py",
   "name": "the unit of judgment was the blank-line block, so one long line failed a paragraph of canonical lines",
   "family": "scan-for-parse",
   "fix_rev": "325b3dcd3",
   "trigger": "a wrapped paragraph with one line left long by an edit",
   "ok": "judged per line, the paragraph reads as mid-edit",
   "bug": "the whole paragraph failed",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "The program arrived here already fixed, in 325b3dcd3, so this tree has no baseline to replay and the record carries no case; the mutant in tools/mutants.py is what holds the fix. Recorded so the shape is not reintroduced when the twin copies are next synced."
  },
  {
   "id": "check-doc-wrap-03",
   "program": "check-doc-wrap.py",
   "name": "the whole-file test called a file with one unwrapped paragraph 11 lines wrong",
   "family": null,
   "fix_rev": "325b3dcd3",
   "trigger": "a wrapped document with one paragraph unwrapped mid-edit",
   "ok": "one paragraph reported as mid-edit, not failed",
   "bug": "11 lines reported wrong",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "The program arrived here already fixed, in 325b3dcd3, so this tree has no baseline to replay and the record carries no case; the mutant in tools/mutants.py is what holds the fix. Recorded so the shape is not reintroduced when the twin copies are next synced."
  },
  {
   "id": "check-doc-wrap-04",
   "program": "check-doc-wrap.py",
   "name": "the failure message named the formatter alone, sending a session round wrap-then-edit-then-red five times",
   "family": "false-comment",
   "fix_rev": "325b3dcd3",
   "trigger": "a paragraph an edit left long, in a document still being edited",
   "ok": "both remedies named, and which one is right depends on whether the document is finished",
   "bug": "the message said to wrap, which the next edit undid",
   "proved": "asserted",
   "harm": "latent",
   "harm_count": 5,
   "discovery": "in-use",
   "notes": "The program arrived here already fixed, in 325b3dcd3, so this tree has no baseline to replay and the record carries no case; the mutant in tools/mutants.py is what holds the fix. Recorded so the shape is not reintroduced when the twin copies are next synced."
  },
  {
   "id": "check-doc-wrap-05",
   "program": "check-doc-wrap.py",
   "name": "a repository tracking no Markdown reported 0 of 0 failed rather than BLOCKED",
   "family": "vacuous-check",
   "fix_rev": "325b3dcd3",
   "trigger": "a run in a repository with no tracked .md file",
   "ok": "BLOCKED at exit 2, nothing checked",
   "bug": "a pass over nothing",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "The program arrived here already fixed, in 325b3dcd3, so this tree has no baseline to replay and the record carries no case; the mutant in tools/mutants.py is what holds the fix. Recorded so the shape is not reintroduced when the twin copies are next synced."
  },
  {
   "id": "check-plan-citations-01",
   "program": "check-plan-citations.py",
   "name": ":0 indexed the file's last line and a backwards range printed its first line as ok",
   "family": "domain-unchecked",
   "fix_rev": "325b3dcd3",
   "trigger": "a citation `file:0` or `file:20-10`",
   "ok": "OUT-OF-RANGE, exit 1",
   "bug": "a line printed as though the citation resolved",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here."
  },
  {
   "id": "check-plan-citations-02",
   "program": "check-plan-citations.py",
   "name": "given several documents only the first was checked, the rest reporting nothing while the run exited 0",
   "family": "quiet-failure",
   "fix_rev": "325b3dcd3",
   "trigger": "two documents on one command line",
   "ok": "each document checked and named",
   "bug": "the second document silently unchecked",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here."
  },
  {
   "id": "check-plan-citations-03",
   "program": "check-plan-citations.py",
   "name": "a dotfile citation such as .hlint.yaml:22 was extracted as hlint.yaml",
   "family": "scan-for-parse",
   "fix_rev": "325b3dcd3",
   "trigger": "a citation into a dotfile",
   "ok": "the dotfile resolves",
   "bug": "a citation into a file that does not exist, failed",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here."
  },
  {
   "id": "check-plan-citations-04",
   "program": "check-plan-citations.py",
   "name": "a document quoting another's stamps failed the plain pass three times over",
   "family": "scan-for-parse",
   "fix_rev": "325b3dcd3",
   "trigger": "a document that quotes a stamp line rather than carrying one",
   "ok": "quotations are not this document's stamp",
   "bug": "three failures for stamps that were never the document's",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here."
  },
  {
   "id": "check-twin-sync-01",
   "program": "check-twin-sync.py",
   "name": "only *.py was compared, so a shared shell script could drift unseen",
   "family": "vacuous-check",
   "fix_rev": "325b3dcd3",
   "trigger": "a shell script under tools/ present in both checkouts and differing",
   "ok": "every other file is compared whole, but the TWIN_SKIP ones",
   "bug": "no drift reported",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "The program arrived here already fixed, in 325b3dcd3, so this tree has no baseline to replay and the record carries no case; the mutant in tools/mutants.py is what holds the fix. Recorded so the shape is not reintroduced when the twin copies are next synced."
  },
  {
   "id": "check-twin-sync-02",
   "program": "check-twin-sync.py",
   "name": "a self-test mutation whose target text was absent from this repo's copy passed vacuously",
   "family": "vacuous-check",
   "fix_rev": "325b3dcd3",
   "trigger": "the configuration case, which replaced a literal TWIN_ROOT only one repo's copy carries",
   "ok": "each mutation asserts its target text exists",
   "bug": "the case compared a copy with itself",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "The program arrived here already fixed, in 325b3dcd3, so this tree has no baseline to replay and the record carries no case; the mutant in tools/mutants.py is what holds the fix. Recorded so the shape is not reintroduced when the twin copies are next synced."
  },
  {
   "id": "heading-outline-01",
   "program": "heading-outline.py",
   "name": "a closing fence was read as a Setext heading's text, reported as '## ```'",
   "family": "scan-for-parse",
   "fix_rev": "325b3dcd3",
   "trigger": "a fenced block followed by a line of dashes",
   "ok": "neither a fence nor its contents underlines",
   "bug": "a heading of three backticks in the outline",
   "proved": "asserted",
   "harm": "latent",
   "harm_count": 1,
   "discovery": "in-use",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here. It fired there; here the pre-fix outline was measured identical to the post-fix one on every tracked document."
  },
  {
   "id": "heading-outline-02",
   "program": "heading-outline.py",
   "name": "a list item was read as a Setext heading's text, reported as '## - item'",
   "family": "scan-for-parse",
   "fix_rev": "325b3dcd3",
   "trigger": "a list item followed by a line of dashes",
   "ok": "a list item does not underline",
   "bug": "the item in the outline as a heading",
   "proved": "asserted",
   "harm": "latent",
   "harm_count": 1,
   "discovery": "in-use",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here. It fired there; here the pre-fix outline was measured identical to the post-fix one on every tracked document."
  },
  {
   "id": "check-doc-wrap-06",
   "program": "check-doc-wrap.py",
   "name": "the fence tracker was one boolean flipped by any fence line, so a backtick fence shown inside a tilde block closed the block and an enumerator after it was reported as fake",
   "family": "scan-for-parse",
   "fix_rev": "325b3dcd3",
   "trigger": "~~~ then a ``` line then `1a. shown` then ~~~",
   "ok": "the fence's kind and length are tracked, and only a closer of the same kind at least as long closes it; no finding",
   "bug": "the ``` line flipped the state, and `1a. shown` was reported as a fake enumerator",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "lint",
   "notes": "The program arrived here already fixed, in 325b3dcd3, so this tree has no baseline to replay and the record carries no case; the mutant in tools/mutants.py is what holds the fix. Recorded so the shape is not reintroduced when the twin copies are next synced."
  },
  {
   "id": "check-plan-citations-05",
   "program": "check-plan-citations.py",
   "name": "a git status that failed read as no dirty file, so --restamp went ahead having verified nothing about the cited files",
   "family": "error-as-value",
   "fix_rev": "325b3dcd3",
   "trigger": "--restamp with a git whose status command fails",
   "ok": "not restamping, git status could not be read, exit 2",
   "bug": "the empty output of the failed status read as clean and the stamp was rewritten, exit 0",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "lint",
   "notes": "Ported with the tool on 2026-09-03. This repo's copy carried the defect until 325b3dcd3, so that commit's parent is its baseline; the bug direction was watched in the horde-ad copy, not here.",
   "plant_cmd": "git init -q . && printf 'line one\\nline two\\n' > a.hs && printf '%s\\n' '`a.hs:1` cited.' '' 'Citations were verified against the tree at commit `0000000aa` (2020-01-01).' > r.md && git add a.hs r.md && git -c user.email=t@t -c user.name=t -c commit.gpgsign=false commit -qm base && mkdir shim && printf '#!/bin/sh\\ncase \"$*\" in *status*) exit 128;; esac\\nexec /usr/bin/git \"$@\"\\n' > shim/git && chmod +x shim/git",
   "env": {
    "PATH": "{tmp}/shim:/usr/bin:/bin"
   },
   "invoke": [
    "r.md",
    "--restamp"
   ],
   "expect_exit": 2,
   "expect_text": "could not be read",
   "bug_exit": 0,
   "bug_text": "->"
  },
  {
   "id": "check-plan-citations-06",
   "program": "check-plan-citations.py",
   "name": "a stamp the formatter wrapped inside a blockquote read as no stamp, so --restamp refused it and the stamp's own orphan and publication checks passed in silence",
   "family": "scan-for-parse",
   "fix_rev": "3a03d4670",
   "trigger": "a stamp whose hash ends one line and whose date opens the next behind a blockquote `>`",
   "ok": "the stamp is found, checked and rewritten, its line break preserved",
   "bug": "no stamp found, exit 1, and the stamp itself never validated",
   "proved": "ran",
   "harm": "fired",
   "harm_count": 2,
   "discovery": "in-use",
   "notes": "Found while restamping this repo's five documents: two of them, docs/leader-desync-bug.md and docs/leader-desync-migration.md, were in exactly that shape, so their stamps had never been checked for orphaning or publication at all. Which break lands where moves whenever a paragraph above is edited, so no document can be kept out of the shape. The horde-ad copy carries the same pattern and the same defect; check-twin-sync could not be run here, that checkout not being mounted.",
   "plant": "a blockquoted stamp wrapped so the hash ends one line and the date opens the next behind a `>`",
   "plant_cmd": "git init -q . && printf 'line one\\nline two\\n' > a.hs && printf '%s\\n' '`a.hs:1` cited.' '' '> Citations were verified against the tree at commit **0000000aa**' '> (2020-01-01).' > r.md && git add a.hs r.md && git -c user.email=t@t -c user.name=t -c commit.gpgsign=false commit -qm base",
   "invoke": [
    "r.md",
    "--restamp"
   ],
   "expect_exit": 0,
   "expect_text": "0000000aa (2020-01-01) ->",
   "bug_exit": 1,
   "bug_text": "no stamp"
  },
  {
   "id": "check-doc-refs-05",
   "program": "check-doc-refs.py",
   "name": "the fence tracker was one boolean flipped by any fence line, so a backtick fence shown inside a tilde block closed it, the prose after was read as command text and the next real block as prose",
   "family": "scan-for-parse",
   "fix_rev": "0123772a2",
   "trigger": "~~~ then a ``` line then ~~~, then prose naming `make X`, then a real ``` block naming `make Y`",
   "ok": "only a tilde fence at least as long closes the block: X is prose and unchecked, Y is a command and fails",
   "bug": "X failed as an unknown target and Y went unchecked, the phase inverted for the rest of the document",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "The shape check-doc-wrap fixed as check-doc-wrap-06, still present in the two other fence trackers of tools/ (heading-outline-03 is the other). Found by a code review on 2026-09-04.",
   "plant_cmd": "git init -q . && printf 'play:\\n\\ttrue\\n' > Makefile && printf '%s\\n' 'A tilde block showing a backtick fence:' '~~~' '```' '~~~' 'so this prose line, make no-such-prose-target, is not a command, while a block after it still is:' '```' 'make no-such-fenced-target' '```' > d.md && git add -A && git -c user.email=t@t -c user.name=t -c commit.gpgsign=false commit -qm c",
   "invoke": [
    "d.md",
    "--without-siblings"
   ],
   "expect_exit": 1,
   "expect_text": "FAIL target make no-such-fenced-target",
   "expect_absent": "no-such-prose-target",
   "bug_exit": 1,
   "bug_text": "FAIL target make no-such-prose-target",
   "bug_absent": "no-such-fenced-target"
  },
  {
   "id": "heading-outline-03",
   "program": "heading-outline.py",
   "name": "the fence tracker was one boolean flipped by any fence line, so a backtick fence inside a tilde block promoted the fenced # lines to headings and swallowed every real heading after it",
   "family": "scan-for-parse",
   "fix_rev": "5ab91b3d0",
   "trigger": "~~~ then a ``` line then `# not a heading` then ~~~ then `## Real`",
   "ok": "the outline holds Real and nothing from inside the block",
   "bug": "the outline held `# not a heading` and dropped Real, exit 0",
   "proved": "ran",
   "harm": "latent",
   "discovery": "generalisation",
   "notes": "From check-doc-wrap-06, by asking where else a fence is tracked; found by a code review on 2026-09-04.",
   "plant_cmd": "printf '~~~\\n```\\n# not a heading\\n~~~\\n## Real\\n' > h.md",
   "invoke": [
    "h.md"
   ],
   "expect_exit": 0,
   "expect_text": "## Real",
   "expect_absent": "not a heading",
   "bug_exit": 0,
   "bug_text": "not a heading",
   "bug_absent": "Real"
  },
  {
   "id": "check-plan-citations-07",
   "program": "check-plan-citations.py",
   "name": "CITE_RE listed neither json nor sh, and check-doc-refs skips every backticked path:NN token as pass 1's, so a citation into a .json or .sh file was checked by neither tool",
   "family": "vacuous-check",
   "fix_rev": "acd696847",
   "trigger": "`d.json:9999` and `s.sh:3` in backticks, both out of range",
   "ok": "both extracted and failed OUT-OF-RANGE",
   "bug": "0 citations checked, exit 0, and check-doc-refs' 0 failed beside it",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "CLAUDE.md names tools/defects.json, so a dangling citation into it would have passed every pass. check-doc-refs' PATH_EXT already carried both extensions; the two lists live in two twin-synced files and cannot be one constant, so each names the other in a comment. Found by a code review on 2026-09-04.",
   "plant_cmd": "git init -q . && printf '{}\\n' > d.json && printf 'a\\nb\\n' > s.sh && printf '%s\\n' 'See `d.json:9999` and `s.sh:3` and `s.sh:1`.' > r.md && git add -A && git -c user.email=t@t -c user.name=t -c commit.gpgsign=false commit -qm c",
   "invoke": [
    "r.md"
   ],
   "expect_exit": 1,
   "expect_text": [
    "OUT-OF-RANGE",
    "ok   s.sh:1"
   ],
   "bug_exit": 0,
   "bug_text": "0 citations checked"
  },
  {
   "id": "check-doc-refs-06",
   "program": "check-doc-refs.py",
   "name": "one boolean answered two questions, whether a sibling is consulted and whether the human waived the stop, so SIBLING_ROOTS = [] degraded every unresolved local path from FAIL to SKIP",
   "family": "other:one-flag-two-questions",
   "fix_rev": "0123772a2",
   "trigger": "SIBLING_ROOTS = [], the docstring's way to disable the sibling check, and a document naming a path that does not exist",
   "ok": "FAIL path, exit 1: local drift is a finding whether or not a sibling is configured",
   "bug": "SKIP path, unresolved locally, and siblings not consulted; 0 failed, exit 0",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "Only --without-siblings, a human's decision, may degrade the verdict; a port with no sibling to consult has nothing to wait for. Found by a code review on 2026-09-04.",
   "plant": "port the program by editing SIBLING_ROOTS to [], then run it over a document naming a path that is not there",
   "plant_cmd": "git init -q . && mkdir engine-src && printf 'ok\\n' > engine-src/Real.hs && printf '%s\\n' '`engine-src/Real.hs` is real and `engine-src/Nope.hs` is drift.' > d.md && git add -A && git -c user.email=t@t -c user.name=t -c commit.gpgsign=false commit -qm c",
   "plant_py": "(ctx.text.index('SIBLING_ROOTS = [\"../lambdahack.github.io\"]'), {'prog': [p for p in [t + '/port.py'] if open(p, 'w').write(ctx.text.replace('SIBLING_ROOTS = [\"../lambdahack.github.io\"]', 'SIBLING_ROOTS = []'))][0]})[1]",
   "invoke": [
    "d.md"
   ],
   "expect_exit": 1,
   "expect_text": "FAIL path   engine-src/Nope.hs",
   "bug_exit": 0,
   "bug_text": "SKIP path   engine-src/Nope.hs"
  },
  {
   "id": "check-doc-refs-07",
   "program": "check-doc-refs.py",
   "name": "main() dispatched --self-test before chdir_root(), so from any directory but the root the self-test resolved its siblings against the wrong place and reported BLOCKED",
   "family": "environment-decides",
   "fix_rev": "0123772a2",
   "trigger": "--self-test from tools/, with the sibling mounted",
   "ok": "the self-test passes as it does from the root",
   "bug": "BLOCKED --- sibling checkout(s) not available, exit 2, so check-all from a subdirectory reported the step as not run",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "check-doc-refs-01 fixed the plain run and left the self-test on the old path; check-doc-examples-04 is the same shape. The case runs in tools/ and needs ../lambdahack.github.io mounted, without which the ok direction is BLOCKED at 2 and reads as a failure. Found by a code review on 2026-09-04.",
   "run_dir": "here",
   "invoke": [
    "--self-test"
   ],
   "expect_exit": 0,
   "expect_text": "every self-test case behaved",
   "bug_exit": 2,
   "bug_text": "BLOCKED"
  },
  {
   "id": "check-doc-examples-04",
   "program": "check-doc-examples.py",
   "name": "self_test() was dispatched before chdir_root, so its from-the-root control ran relative to the caller's directory and the subdirectory-agreement row fired on an unbroken checker",
   "family": "environment-decides",
   "fix_rev": "43703cc24",
   "trigger": "--self-test from tools/",
   "ok": "self-test: PASS, as from the root",
   "bug": "the here run got tools/README.md and exited 2, the there run exited 0, and the self-test reported that a run from a subdirectory disagrees with one from the root, exit 1",
   "proved": "ran",
   "harm": "latent",
   "discovery": "generalisation",
   "notes": "From check-doc-refs-07, by asking which other main() dispatches the self-test first: check-doc-wrap and check-plan-citations do too, and pass from a subdirectory, neither self-test naming a root-relative path. Found by a code review on 2026-09-04.",
   "run_dir": "here",
   "invoke": [
    "--self-test"
   ],
   "expect_exit": 0,
   "expect_text": "self-test: PASS",
   "bug_exit": 1,
   "bug_text": "disagrees with one from the root"
  },
  {
   "id": "checks-01",
   "program": "checks.py",
   "name": "the shellcheck step printed nothing and exited 0 with no tracked .sh file, so check-all's ok line read like a lint that had run",
   "family": "vacuous-check",
   "fix_rev": "014101b77",
   "trigger": "the step's bash string with `git ls-files '*.sh'` empty, the case today",
   "ok": "the step says no tracked shell script; nothing linted, and exits 0",
   "bug": "no output at all",
   "proved": "ran",
   "harm": "fired",
   "discovery": "review",
   "notes": "CLAUDE.md's standing checks already claimed the step says so, since 2026-09-03; every check-all run until this fix printed the empty tail. Found by a code review on 2026-09-04.",
   "unit": "__import__('subprocess').run([a.replace('{' + 'root}', '{root}') for a in dict(STEPS)['shellcheck']], capture_output=True, text=True).stdout",
   "expect_text": "nothing linted",
   "bug_text": "''"
  },
  {
   "id": "checks-02",
   "program": "checks.py",
   "name": "the DOCS loop exited 0 after zero iterations when its cd or its git ls-files failed",
   "family": "other:dropped-status",
   "fix_rev": "014101b77",
   "trigger": "the DOCS string with {root} naming a directory that does not exist",
   "ok": "exit 2: the pass did not happen",
   "bug": "cd's error on stderr and exit 0",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "Unreachable from check-all, whose {root} is the directory it read checks.py from; a moved or deleted checkout between the read and the step is the only route. Found by a code review on 2026-09-04.",
   "unit": "__import__('subprocess').run(['bash', '-c', (DOCS % 'check-doc-wrap.py').replace('{' + 'root}', '/nonexistent')], capture_output=True, text=True).returncode",
   "expect_text": "2",
   "bug_text": "0"
  },
  {
   "id": "checks-03",
   "program": "checks.py",
   "name": "the pyflakes step's failure text named `command -v pyflakes`, a probe it never runs and one that finds nothing on this machine while the module it does probe answers",
   "family": "false-comment",
   "fix_rev": "014101b77",
   "trigger": "python3 -m pyflakes --version failing",
   "ok": "the message names the probe that ran",
   "bug": "a reader sent to install a tool that is present",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Never fired: pyflakes has answered on every run. Found by a code review on 2026-09-04."
  },
  {
   "id": "leader-census-01",
   "program": "leader-census.py",
   "name": "TREE and DOC are root-relative but main() neither moved to the root nor checked them, so a run from elsewhere died with a traceback at exit 1 or, given an absolute DOC, walked a missing TREE and reported every bucketed name absent",
   "family": "environment-decides",
   "fix_rev": "b1c3253f6",
   "trigger": "a run from a directory that is not the repository root",
   "ok": "the script moves to the root; outside a repository it says no such document and exits 2",
   "bug": "FileNotFoundError, exit 1, a findings code",
   "proved": "ran",
   "harm": "latent",
   "discovery": "generalisation",
   "notes": "From check-doc-refs-01, the same rule every other tools/ script already follows; this one is temporary and had been left out. Found by a code review on 2026-09-04.",
   "invoke": [],
   "expect_exit": 2,
   "expect_text": "no such document",
   "bug_exit": 1,
   "bug_text": "FileNotFoundError"
  },
  {
   "id": "heading-outline-04",
   "program": "heading-outline.py",
   "name": "any leading --- was read as opening YAML frontmatter, so a document opening with a rule lost every heading up to the next rule",
   "family": "scan-for-parse",
   "fix_rev": "5ab91b3d0",
   "trigger": "---, a blank line, a Setext-2 heading, then `## Later`",
   "ok": "both headings in the outline",
   "bug": "the Setext heading's underline closed the frontmatter and the heading was dropped, exit 0",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "No tracked document opens with a rule today. Found by a code review on 2026-09-04.",
   "plant_cmd": "printf -- '---\\n\\nTitle\\n---\\n\\n## Later\\n' > h.md",
   "invoke": [
    "h.md"
   ],
   "expect_exit": 0,
   "expect_text": [
    "## Title",
    "## Later"
   ],
   "bug_exit": 0,
   "bug_absent": "Title"
  },
  {
   "id": "check-twin-sync-03",
   "program": "check-twin-sync.py",
   "name": "the self-test's mutated-code case anchored on a literal that occurs only in its own source, so it mutated the self-test in the twin rather than a line of comparable()",
   "family": "vacuous-check",
   "fix_rev": "1df971658",
   "trigger": "the code case's anchor, `\"\"\"The part of a script the twins must agree on.\"\"\"`, against comparable()'s two-line docstring",
   "ok": "the anchor is a code line of comparable(), found inside that function by its AST range rather than by first occurrence, and the case asserts it there",
   "bug": "the case still bit, any code line below the docstring being drift, but proved nothing about comparable()",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Found by a code review on 2026-09-04.",
   "plant": "read self_test's code case and grep its anchor: it must occur in comparable() and nowhere in self_test but the call"
  },
  {
   "id": "check-plan-citations-08",
   "program": "check-plan-citations.py",
   "name": "all_files_named spawned one find over the source roots per citation that did not resolve directly, 1.1 s against 0.3 s on the wasm plan",
   "family": "other:repeated-work",
   "fix_rev": "acd696847",
   "trigger": "two citations of one basename through /.../ or a bare name",
   "ok": "one find per basename, the rest served from the cache",
   "bug": "a find per citation",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "Found by a code review on 2026-09-04; the timing is the review's, on docs/wasm-frontend-unified-plan.md.",
   "unit": "(resolve('no/.../Nope.hs'), resolve('no/.../Nope.hs'), 'hits=%d' % all_files_named.cache_info().hits)[2]",
   "expect_text": "'hits=1'",
   "bug_text": "AttributeError"
  },
  {
   "id": "Makefile-01",
   "program": "Makefile",
   "name": "serve-wasm chained its cd with `;`, so a failed cd did not stop the recipe",
   "family": "unverified-state",
   "fix_rev": "7b84ed60d",
   "trigger": "`make serve-wasm` with `../lambdahack.github.io` absent, which the outer sandbox wrapper also produces by not mounting it",
   "ok": "the recipe stops at the cd and make reports the missing checkout",
   "bug": "the cd's failure was skipped and `node serve.mjs . 8080` ran in the repo root, where no serve.mjs exists, so make failed on node's missing-module error instead of on the absent checkout",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "Found by a blind reader in the docs/wasm-frontend-unified-plan.md review of 2026-09-04, which reported it as serving the repo root on :8080; that consequence is wrong here, no serve.mjs existing at the root, and the real cost is a misleading diagnostic. It would become a wrong-tree serve if a serve.mjs ever sat at the root. The same `;`-to-`&&` sweep had already fixed build-ts in 2b4b7d71f and stopped there. The corpus root is `tools/`, so `defect-run.py` resolves a program under it and cannot reach one at the repository root: this is a tier-1 record, not a replayable case."
  },
  {
   "id": "check-doc-examples-05",
   "program": "check-doc-examples.py",
   "name": "the subdirectory-agreement row compared two runs over the literal README.md, so with README.md absent both exited 2 alike and the row passed",
   "family": "vacuous-check",
   "fix_rev": "f282d1c92",
   "trigger": "--self-test in a clone with README.md removed",
   "ok": "the row fails, saying its from-the-root run did not happen",
   "bug": "self-test: PASS",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported from horde-ad on 2026-09-04, where a code review found it; this copy carried the defect until the port commit, so the audit replays the bug direction here. No runnable case: it needs README.md gone, which a case may not do to the tree; the mutant in mutants.py moves the clone's aside for its judge.",
   "plant": "clone the repository, remove README.md, run --self-test: it must report that the from-the-root control did not run"
  },
  {
   "id": "checks-04",
   "program": "checks.py",
   "name": "the DOCS loop kept worst at 0 for a checker status outside 0, 1 and 2 -- a killed checker or an absent interpreter -- so the step read ok",
   "family": "other:dropped-status",
   "fix_rev": "f282d1c92",
   "trigger": "the DOCS string with a checker whose run ends in 127 or 137",
   "ok": "the status reaches check-all as it is, one checker run standing for the step, and anything but 0 and 2 is a failure there",
   "bug": "exit 0",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported from horde-ad on 2026-09-04, where a code review found it; this copy carried the defect until the port commit, so the audit replays the bug direction here. The re-aggregating loop then went altogether: each checker takes the whole list and aggregates it itself.",
   "unit": "__import__('subprocess').run(['bash', '-c', (DOCS % 'nothing.py 2>/dev/null || no-such-command-xyz').replace('{' + 'root}', '{root}')], capture_output=True, text=True).returncode",
   "expect_text": "127",
   "bug_text": "0"
  },
  {
   "id": "checks-05",
   "program": "checks.py",
   "name": "the DOCS loop split the listing on whitespace, so a tracked document with a space in its name reached the checker as two names",
   "family": "scan-for-parse",
   "fix_rev": "f282d1c92",
   "trigger": "a repository tracking `a b.md`",
   "ok": "the checker is handed `a b.md`",
   "bug": "it is handed `a`, then `b.md`",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported from horde-ad on 2026-09-04, where a code review found it; this copy carried the defect until the port commit, so the audit replays the bug direction here. No document here carries a space, so it never fired.",
   "plant_cmd": "git init -q c && mkdir c/tools && printf 'import os, sys\\nsys.exit(0 if os.path.isfile(sys.argv[1]) else 1)\\n' > c/tools/seen.py && printf 'x\\n' > 'c/a b.md' && git -C c add -A && git -C c -c user.email=t@t -c user.name=t -c commit.gpgsign=false commit -qm c",
   "unit": "__import__('subprocess').run(['bash', '-c', (DOCS % 'seen.py').replace('{' + 'root}', '{tmp}/c/tools')], capture_output=True, text=True).returncode",
   "expect_text": "0",
   "bug_text": "1"
  },
  {
   "id": "check-doc-wrap-07",
   "program": "check-doc-wrap.py",
   "name": "fake_markers exempted enumerator-like lines inside fenced blocks only, so a line of an indented code block was reported as a fake enumerator",
   "family": "scan-for-parse",
   "fix_rev": "f282d1c92",
   "trigger": "a paragraph, a blank line and `    x) echo hi;;`",
   "ok": "no finding: an indented line after a blank one is code",
   "bug": "FAIL: line 3 opens with an enumerator Markdown does not read as one",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported from horde-ad on 2026-09-04, where a code review found it; this copy carried the defect until the port commit, so the audit replays the bug direction here. Latent, no tracked document carrying such a line. The exemption is the indented-code rule, four spaces after a blank line; a nested list's fake enumerator under a blank line is read as code too, and CommonMark reads it as a paragraph either way.",
   "unit": "fake_markers('para\\n\\n    x) echo hi;;\\n')",
   "expect_text": "[]",
   "bug_text": "(3, 'x) echo hi;;')"
  },
  {
   "id": "check-doc-wrap-08",
   "program": "check-doc-wrap.py",
   "name": "a fence line indented four or more spaces opened or closed a fenced block, where CommonMark reads it as indented code inside or outside one",
   "family": "scan-for-parse",
   "fix_rev": "f282d1c92",
   "trigger": "a paragraph, a blank line, `    ```` and then `1a. shown`",
   "ok": "the indented line is code and the enumerator after it is reported; inside an open block an indented fence line is content and does not close it",
   "bug": "the indented line opened a fence and hid the enumerator; inside a block it closed the block and the enumerator after it was reported",
   "proved": "ran",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported from horde-ad on 2026-09-04, found there reviewing check-doc-wrap-07, whose indented-code rule sat below the fence test and so never saw an indented fence line; this copy carried the defect until the port commit, so the audit replays the bug direction here. A fence takes up to three spaces of indentation, as CommonMark has it; no tracked document carries an indented fence line.",
   "unit": "fake_markers('para\\n\\n    ```\\n1a. shown\\n')",
   "expect_text": "(4, '1a. shown')",
   "bug_text": "[]"
  },
  {
   "id": "mutants-01",
   "program": "mutants.py",
   "name": "its docstring said the mutants replace the dated broken-in-a-copy proofs in the checkers' docstrings, while the checkers still carried them",
   "family": "false-comment",
   "fix_rev": "f282d1c92",
   "trigger": "reading a checker's docstring beside this file's",
   "ok": "each checker's docstring points here for its proofs",
   "bug": "the dated sentences stood, expiring with every edit under them and saying nothing",
   "proved": "asserted",
   "harm": "latent",
   "discovery": "review",
   "notes": "Ported from horde-ad on 2026-09-04, where a code review found it. Prose, so no case."
  },
  {
   "id": "check-plan-crossrefs-01",
   "kind": "control",
   "program": "check-plan-crossrefs.py",
   "name": "the plan at 91f28c8f3, before the 2026-09-04 campaign's fixes, draws the graph findings that campaign found by hand: A1 at 0.0, R4 and R6, A2 on run-wasm-game.mjs, A3 at 1.2 and 1.5, A4 at 0.3, 2.2, 2.3 and 2.4",
   "notes": "The historical corpus, so the checker's proof is the document's own defects rather than a planted one. The handoff also expected A3 at 2.5, and it is not drawn: 2.5's Split at that revision restated R6's deliverable in prose and never named render-coherence.test.ts, so there is no token to read and the finding was a reader's inference. A control, not a defect: the defects were the document's, and the corpus root is tools/, which defect-run.py cannot reach the document from.",
   "plant_cmd": "git -C {repo} show 91f28c8f3:docs/wasm-frontend-unified-plan.md > plan.md && git -C {repo} show 91f28c8f3:tools/doc-refs-allow.txt > allow.txt",
   "invoke": [
    "plan.md",
    "--allowlist",
    "allow.txt"
   ],
   "expect_exit": 1,
   "expect_text": [
    "A1 `ts-src/src/terminal-input.test.ts` --- claimed by no item; written by 0.0 and 0.1, and 0.0's **Owns** does not name `tools/doc-refs-allow.txt`",
    "A1 `url-options-core.ts` --- claimed by R4, whose **Owns** does not name `tools/doc-refs-allow.txt`",
    "A1 `render-coherence.test.ts` --- claimed by R6, whose **Owns** does not name `tools/doc-refs-allow.txt`",
    "A2 `run-wasm-game.mjs` --- 3.2's **Owns** names neither 1.2",
    "A3 1.2 --- writes `run-wasm-game.mjs`, created by 0.3, which its depends-on cell does not reach",
    "A3 1.5 --- writes `run-wasm-game.mjs`, created by 0.3, which its depends-on cell does not reach",
    "A4 0.3 --- **Owns** names `tools/doc-refs-allow.txt` and **Done** lacks `docs`",
    "A4 2.2 --- **Owns** names `docs/wasm-frontend-unified-plan.md` and **Done** lacks `docs`",
    "A4 2.3 --- **Owns** names `docs/wasm-frontend-unified-plan.md` and **Done** lacks `docs`",
    "A4 2.4 --- **Owns** names `docs/wasm-frontend-unified-plan.md` and **Done** lacks `docs`"
   ]
  },
  {
   "id": "check-plan-crossrefs-02",
   "kind": "control",
   "program": "check-plan-crossrefs.py",
   "name": "the same findings are silent over the plan as committed, so each assertion is loud at one end of the history and quiet at the other",
   "notes": "The complement of check-plan-crossrefs-01: an assertion silent at both ends is not working, and one loud at both ends is either right or wrong about the live document, which is read before the checker is touched. The exit status is not asserted, the live document carrying findings of its own.",
   "invoke": [
    "{repo}/docs/wasm-frontend-unified-plan.md",
    "--allowlist",
    "{repo}/tools/doc-refs-allow.txt"
   ],
   "expect_absent": [
    "A1 `ts-src/src/terminal-input.test.ts` --- claimed by no item; written by 0.0 and 0.1, and 0.0's **Owns** does not name `tools/doc-refs-allow.txt`",
    "A1 `url-options-core.ts` --- claimed by R4, whose **Owns** does not name `tools/doc-refs-allow.txt`",
    "A1 `render-coherence.test.ts` --- claimed by R6, whose **Owns** does not name `tools/doc-refs-allow.txt`",
    "A2 `run-wasm-game.mjs` --- 3.2's **Owns** names neither 1.2",
    "A3 1.2 --- writes `run-wasm-game.mjs`, created by 0.3, which its depends-on cell does not reach",
    "A3 1.5 --- writes `run-wasm-game.mjs`, created by 0.3, which its depends-on cell does not reach",
    "A4 0.3 --- **Owns** names `tools/doc-refs-allow.txt` and **Done** lacks `docs`",
    "A4 2.2 --- **Owns** names `docs/wasm-frontend-unified-plan.md` and **Done** lacks `docs`",
    "A4 2.3 --- **Owns** names `docs/wasm-frontend-unified-plan.md` and **Done** lacks `docs`",
    "A4 2.4 --- **Owns** names `docs/wasm-frontend-unified-plan.md` and **Done** lacks `docs`"
   ]
  },
  {
   "id": "check-plan-crossrefs-03",
   "kind": "control",
   "program": "check-plan-crossrefs.py",
   "name": "the migration document reads clean under its own grammar, and the run says A1 had nothing to read rather than passing an empty allowlist block",
   "notes": "The pointman work list took the execution-block grammar on 2026-09-04 (its sec. 00 Log), and this pins the shape it must keep while it is live and edited: silent alone. A control rather than a defect, nothing having been fixed; what the joint run over both documents sees is pinned apart.",
   "invoke": [
    "{repo}/docs/leader-desync-migration.md"
   ],
   "expect_exit": 0,
   "expect_text": [
    "A1 not run over docs/leader-desync-migration.md"
   ]
  }
 ]
}
